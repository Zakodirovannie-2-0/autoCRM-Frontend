import '../index.css'

const LoginPage = () => {
    return (
        <div className={'flex flex-row min-h-screen justify-center items-center'}>
            <h1 className={'flex text-center text-4xl'}>Temporary page container</h1>
        </div>
    );
};

export default LoginPage;